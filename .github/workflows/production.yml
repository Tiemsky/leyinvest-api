name: Production Deployment - Laravel API

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      skip_tests:
        description: 'Skip tests (emergency deployment only)'
        required: false
        default: 'false'

env:
  REGISTRY: ghcr.io
  # Nom de l'image spécifique au service Laravel
  IMAGE_NAME: ${{ github.repository }}/laravel-api
  HOST_DIR: /var/www/production
  API_PROXY_PORT: 8001 # Port exposé sur l'hôte pour Dokploy

jobs:

  # ----------------------------------------------------
  # Job 1: Tests Unitaires et Fonctionnels
  # ----------------------------------------------------
  test:
    runs-on: ubuntu-latest
    if: github.event.inputs.skip_tests != 'true'

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: laravel_test
          POSTGRES_USER: laravel
          POSTGRES_PASSWORD: secret
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ✅ Ajout des étapes manquantes (Setup PHP et Composer)
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, dom, fileinfo, pgsql, redis
          coverage: xdebug

      - name: Install Dependencies
        # Crée le dossier 'vendor' essentiel pour 'php artisan'
        run: composer install --prefer-dist --no-progress --no-suggest --optimize-autoloader

      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.testing', '.env');"

      - name: Generate key
        run: php artisan key:generate

      - name: Directory Permissions
        # Assure les droits d'écriture pour l'utilisateur runner
        run: chmod -R 777 storage bootstrap/cache

      - name: Run Tests
        env:
          DB_CONNECTION: pgsql
          # Utiliser les noms de services Docker
          DB_HOST: postgres
          DB_PORT: 5432
          DB_DATABASE: laravel_test
          DB_USERNAME: laravel
          DB_PASSWORD: secret
          REDIS_HOST: redis
          REDIS_PORT: 6379
        run: php artisan test --coverage --min=80

      - name: Security Audit
        run: composer audit


  # ----------------------------------------------------
  # Job 2: Build et Push de l'Image Docker
  # ----------------------------------------------------
  build:
    needs: test
    runs-on: ubuntu-latest
    if: always() && (needs.test.result == 'success' || needs.test.result == 'skipped' || github.event.inputs.skip_tests == 'true')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Configuration multi-architecture (ARM64 pour Hetzner)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (Tags)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=prod-
            type=raw,value=latest

      - name: Build and push Docker image (Multi-Arch)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./laravel-api/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_ENV=production
          cache-from: type=gha
          cache-to: type=gha,mode=max



  # ----------------------------------------------------
  # Job 3: Déploiement Production (Zero-Downtime)
  # ----------------------------------------------------
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Setup SSH (Known Hosts inclus)
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}

      - name: Set Deployment Environment Variables
        id: env_vars
        run: |
          # Regrouper les secrets DB/APP nécessaires au docker-compose prod
          echo "DB_SECRETS=POSTGRES_DB=${{ secrets.PROD_POSTGRES_DB }} POSTGRES_USER=${{ secrets.PROD_POSTGRES_USER }} POSTGRES_PASSWORD=${{ secrets.PROD_POSTGRES_PASSWORD }} MYSQL_DATABASE=${{ secrets.PROD_MYSQL_DATABASE }} MYSQL_USER=${{ secrets.PROD_MYSQL_USER }} MYSQL_PASSWORD=${{ secrets.PROD_MYSQL_PASSWORD }} MYSQL_ROOT_PASSWORD=${{ secrets.PROD_MYSQL_ROOT_PASSWORD }} PGADMIN_DEFAULT_EMAIL=${{ secrets.PROD_PGADMIN_EMAIL }} PGADMIN_DEFAULT_PASSWORD=${{ secrets.PROD_PGADMIN_PASSWORD }} APP_KEY=${{ secrets.PROD_APP_KEY }}" >> $GITHUB_OUTPUT
          echo "SERVICES_TO_UPDATE=laravel_app queue scheduler api_proxy" >> $GITHUB_OUTPUT

      - name: Enable Maintenance Mode
        env:
          MAINTENANCE_SECRET: ${{ secrets.MAINTENANCE_SECRET }}
        run: |
          ssh ${{ secrets.PROD_USER }}@${{ secrets.PROD_HOST }} << 'EOF'
            set -e
            cd ${HOST_DIR}
            # Utiliser le service Laravel pour le mode maintenance
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan down --retry=60 --secret="${MAINTENANCE_SECRET}"
            echo "🔒 Maintenance mode enabled"
          EOF

      - name: Create Production Backup (PostgreSQL seulement)
        env:
          DB_USER: ${{ secrets.PROD_POSTGRES_USER }}
          DB_NAME: ${{ secrets.PROD_POSTGRES_DB }}
        run: |
          ssh ${{ secrets.PROD_USER }}@${{ secrets.PROD_HOST }} << 'EOF'
            set -e

            BACKUP_DIR="/var/backups/laravel/production"
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            APP_DIR="${HOST_DIR}"
            RETENTION_DAYS=30

            mkdir -p $BACKUP_DIR
            echo "🗄️ Creating PostgreSQL DB backup..."
            docker exec prod_postgres pg_dump -U ${DB_USER} -Fc ${DB_NAME} > $BACKUP_DIR/db_${TIMESTAMP}.dump

            echo "📁 Backing up storage..."
            # On assume que le volume 'laravel_storage' est monté dans HOST_DIR
            tar -czf $BACKUP_DIR/storage_${TIMESTAMP}.tar.gz -C $APP_DIR laravel_storage

            echo "🧹 Cleaning backups..."
            find $BACKUP_DIR -type f -mtime +$RETENTION_DAYS -delete

            echo "✅ Backup completed: ${TIMESTAMP}"
          EOF

      - name: Deploy to Production Server
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_ACTOR: ${{ github.actor }}
          DB_SECRETS: ${{ steps.env_vars.outputs.DB_SECRETS }}
        run: |
          ssh ${{ secrets.PROD_USER }}@${{ secrets.PROD_HOST }} << 'EOF'
            set -e

            cd ${HOST_DIR}

            # Login to GitHub Container Registry
            echo $GH_TOKEN | docker login ghcr.io -u $GH_ACTOR --password-stdin

            # Pull latest image
            echo "📦 Pulling latest image..."
            docker-compose -f docker-compose.prod.yml pull laravel_app queue scheduler

            echo "🚀 Starting new version (Zero-Downtime)..."

            # 🎯 CORRECTION: Utiliser la syntaxe correcte pour définir les variables d'environnement dans le shell SSH
            # Cette méthode garantit que les secrets sont passés à docker-compose en tant qu'environnement
            eval "$(echo "${DB_SECRETS}" | xargs -n 1)"

            # Mise à jour des services Laravel et du proxy
            docker-compose -f docker-compose.prod.yml up -d --force-recreate ${{ steps.env_vars.outputs.SERVICES_TO_UPDATE }}

            echo "⏳ Waiting for healthchecks to pass..."
            sleep 15

            echo "🔄 Running database migrations..."
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan migrate --force

            # Clear and optimize caches
            echo "🔧 Optimizing application..."
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan optimize:clear
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan optimize

            # Restart queue workers gracefully
            echo "🔄 Restarting queue workers..."
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan queue:restart

            echo "✅ Deployment completed!"
          EOF

      - name: Smoke Tests (via Dokploy port 8001)
        env:
          API_PROXY_PORT: ${{ env.API_PROXY_PORT }}
        run: |
          echo "🧪 Running smoke tests..."

          # Test Laravel
          response=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.PROD_HOST }}:${API_PROXY_PORT}/laravel/health || echo "000")
          if [ "$response" != "200" ]; then
            echo "❌ Laravel health check failed with status: $response"
            exit 1
          fi

          # Test FastAPI (pour s'assurer que le proxy fonctionne sur l'autre application)
          api_response=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.PROD_HOST }}:${API_PROXY_PORT}/fastapi/health || echo "000")
          if [ "$api_response" != "200" ]; then
            echo "❌ FastAPI health check failed with status: $api_response"
            exit 1
          fi

          echo "✅ All smoke tests passed!"

      - name: Disable Maintenance Mode
        if: success()
        run: |
          ssh ${{ secrets.PROD_USER }}@${{ secrets.PROD_HOST }} << 'EOF'
            cd ${HOST_DIR}
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan up
            echo "🔓 Maintenance mode disabled"
          EOF

      - name: Notify Success
        if: success()
        run: |
          echo "✅ Production deployment successful!"
          # ... (Notification webhook)




  # ----------------------------------------------------
  # Job 4: Rollback d'urgence
  # ----------------------------------------------------
  rollback:
    runs-on: ubuntu-latest
    if: failure()
    needs: [deploy]

    steps:
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}

      - name: Emergency Rollback
        env:
          DB_USER: ${{ secrets.PROD_POSTGRES_USER }}
          DB_NAME: ${{ secrets.PROD_POSTGRES_DB }}
        run: |
          ssh ${{ secrets.PROD_USER }}@${{ secrets.PROD_HOST }} << 'EOF'
            set -e

            cd ${HOST_DIR}
            BACKUP_DIR="/var/backups/laravel/production"

            LATEST_DB=$(ls -t $BACKUP_DIR/db_*.dump | head -n1)
            LATEST_STORAGE=$(ls -t $BACKUP_DIR/storage_*.tar.gz | head -n1)

            echo "🔙 EMERGENCY ROLLBACK INITIATED..."

            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan down || true

            # Restauration DB PostgreSQL
            echo "🗄️ Restoring database from: $LATEST_DB"
            docker exec -i prod_postgres pg_restore -U ${DB_USER} -d ${DB_NAME} --clean < $LATEST_DB

            # Restauration Stockage
            echo "📁 Restoring storage from: $LATEST_STORAGE"
            tar -xzf $LATEST_STORAGE -C ${HOST_DIR}

            # Rollback de l'image Docker (Redéploiement forcé de la dernière image fonctionnelle)
            echo "🐳 Rolling back Docker images..."
            docker-compose -f docker-compose.prod.yml up -d --force-recreate laravel_app queue scheduler api_proxy

            # Nettoyer et désactiver le mode maintenance
            sleep 10
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan optimize:clear
            docker-compose -f docker-compose.prod.yml exec -T laravel_app php artisan up

            echo "✅ Rollback completed!"
          EOF

      - name: Notify Failure
        if: always()
        run: |
          echo "❌ Production deployment failed - Rollback executed!"
          # ... (Notification critique)
